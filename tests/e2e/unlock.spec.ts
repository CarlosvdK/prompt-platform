import { test, expect } from '@playwright/test'

test.describe('Unlock flow', () => {
  test('should show unlock gate on prompt detail', async ({ page }) => {
    // TODO: Navigate to a published prompt and verify unlock gate
    test.skip()
  })

  test('should complete mock unlock flow', async ({ page }) => {
    // TODO: Click unlock, wait for mock ad, verify content revealed
    test.skip()
  })
})
